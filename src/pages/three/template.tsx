import React, { memo, useEffect } from "react";

/** @name 模版 */
const ThreeTemplate = (props: any) => {
  useEffect(() => {
    // 创建场景
    // 创建相机
    // 创建渲染器
    // 1. 创建
    // 2. 设置渲染器宽度
    // 3. 将渲染内容添加到节点里面
    // 创建材质
    // 创建几何体
    // 组合材质和几何体
    // 将组合添加到场景中
  }, []);
  return <div>DrawText</div>;
};

export default memo(ThreeTemplate);
